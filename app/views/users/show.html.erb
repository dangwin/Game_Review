<h1> Welcome <%= @user.full_name %></h1>

<h3>Here are your games</h3>

<% @games.each do |game| %>
<ul>
    <li><%=link_to game.title, game_path(game) %> <%=link_to "Delete", game_path(game), method: :delete, confirm: "Are you sure you want to delete this game?" %></li>
    </ul>
<% end %>


<%= link_to "Add a New Game", new_game_path %><br><br>
<li><%= link_to "LogOut", logout_url, method: :delete %></li>

